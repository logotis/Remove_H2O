import 'package:flutter/material.dart';

class shrappage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Sharepage"),
      ),
    );
  }
}
